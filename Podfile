use_frameworks!

platform :ios, '8.0'

target 'LHNetworking_Example' do
  pod 'LHNetworking', :path => '../'

  target 'LHNetworking_Tests' do
    inherit! :search_paths

    pod 'FBSnapshotTestCase'
  end
end
